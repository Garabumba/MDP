@using MDPActual.Models;

@model IEnumerable<Item>
@{
	Layout = null;
}

@if (@Model.Count() > 0)
{
	@foreach(Item i in Model)
	{
		@*@if(i.Email == "Философия")
		{*@
			<h1>@i.Name</h1>
			<h1>@i.Email</h1>
			<h1>@i.Roll</h1>
		@*}*@
	}
}